//ValueNotifier will hold the data
//ValueListenableBuilder will listen to data change

import 'package:flutter/material.dart';

ValueNotifier<int> selectedPageNotifier = ValueNotifier(0);
ValueNotifier<int> isDarkModeNotifier = ValueNotifier(0);

